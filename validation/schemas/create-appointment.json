{
    "type": "object",
    "properties": {
        "patientId": {
            "type": "string",
            "pattern": "^[0-9a-fA-F]{8}$"
        },
        "date": {
            "type": "string"
        },
        "kind": {
            "type": "string"
        },
        "practitioner": {
            "type": "string",
            "pattern": "^[0-9a-fA-F]{8}$"
        }
    },
    "required": [
        "patientId",
        "date",
        "kind",
        "practitioner"
    ],
    "additionalProperties": false
}